name: Deploy to Render

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Render Deploy Hook
        env:
          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          if [ -n "$RENDER_DEPLOY_HOOK_URL" ]; then
            echo "🚀 Triggering Render deployment..."
            curl -sf -X POST "$RENDER_DEPLOY_HOOK_URL" && echo "✅ Deploy hook triggered!" || echo "⚠️ Deploy hook failed — check Render dashboard"
          else
            echo "⚠️ RENDER_DEPLOY_HOOK_URL not set — skipping hook trigger"
            echo "Add secret at: https://github.com/breverdbidder/zonewise-agents/settings/secrets/actions"
            echo "Get hook URL from: Render Dashboard → Service → Settings → Deploy Hook"
          fi

      - name: Deployment Status
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 ZoneWise Agents Deployment Initiated"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Health Check: https://zonewise-agents.onrender.com/health"
          echo "API Docs: https://zonewise-agents.onrender.com/docs"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
